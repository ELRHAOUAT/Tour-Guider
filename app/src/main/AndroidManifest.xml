<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tourguider2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".manageProfile"></activity>
        <activity android:name=".vue.ExploreNadorActivity" />
        <activity android:name=".vue.ResetPasswordActivity" />
        <activity android:name=".vue.ForgotPassword" />
        <activity android:name=".vue.CitysActivity" />
        <activity android:name=".vue.RegisterActivity" />
        <activity android:name=".vue.LoginActivity" />
        <activity android:name=".vue.ChooseActivity" />
        <activity android:name=".vue.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>